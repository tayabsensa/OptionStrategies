import React from "react";
import StrategyDetail from "../Components/StrategyDetail";

const StrategyDetailPage = ({ item }) => {
  return <StrategyDetail item={item} />;
};

export default StrategyDetailPage;
